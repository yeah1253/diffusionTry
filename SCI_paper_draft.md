# 面向极端工况轴承故障诊断的物理引导多源条件扩散生成与硬件在环验证

## 摘要

工业旋转机械长期运行在变负载、变转速和强噪声环境中，轴承故障样本往往具有明显的工况依赖性。然而，在真实工业现场中，低速低载、重载冲击、早期微弱损伤等边缘工况样本难以主动采集，且故障注入实验成本高、风险大，导致数据驱动诊断模型在极端工况下缺乏充分验证。针对上述问题，本文提出一种融合物理先验与多源条件信息的一维扩散生成框架 PhysiNet，并进一步构建硬件在环（hardware-in-the-loop, HIL）实时诊断验证平台，用于评估生成数据对无样本或少样本变工况故障诊断的增强作用。所提方法首先从 SDUST 轴承数据集中提取转速、负载和故障类型等工况信息，将转速和负载归一化为条件向量，并通过轴承动力学机理模型生成与故障部位及转速匹配的物理先验信号。随后，PhysiNet 采用异构条件联合嵌入网络，将工况条件 token 与物理机理序列在 Transformer 编码器中进行深度融合，并通过可微分连续小波变换模块动态提取物理故障分量。为缓解生成信号与真实信号在低维流形上的分布偏离，本文进一步引入基于 KL 散度的流形对齐损失，使扩散去噪目标同时约束点态重构误差与批内分布结构。实验部分基于真实数据与生成数据构建混合训练集，并采用曼哈顿距离梯度配比策略模拟核心工况到边缘工况的真实样本递减过程。离线评估和 HIL 实时测试结果表明，生成数据能够有效补充边缘工况样本，提高 CNN、TCN、ResNet、Conformer、MSDCNN、RepVGG 等多种诊断网络在极端工况和动态变工况场景下的稳定性。本文工作为工业轴承故障诊断中真实样本稀缺、极端工况难验证和边缘部署可靠性不足等问题提供了一种可复现的生成增强与工程验证方案。

**关键词**：轴承故障诊断；扩散模型；物理先验；条件生成；硬件在环；变工况诊断

## 1 引言

### 1.1 研究背景与动机

滚动轴承是电机、泵、压缩机、风电传动链和高速加工装备中的关键支撑部件，其健康状态直接影响整机的安全性与运行效率。振动信号由于对冲击、调制和周期性故障成分敏感，被广泛用于轴承故障诊断 [1], [2]。近年来，卷积神经网络、时间卷积网络、Transformer 以及轻量化网络在轴承故障识别任务中取得了较高精度 [3]-[6]，但这类模型通常依赖覆盖充分的真实训练样本。当测试工况与训练工况存在显著差异时，模型容易学习到与工况相关的伪特征，而非稳定的故障机理特征。

在真实工业系统中，故障数据稀缺主要来自三个方面。首先，健康设备通常不允许人为制造故障，真实故障只能在长期运行中被动积累。其次，低速低载、突变负载和边界转速等极端工况出现频率低，采集成本高，且不同故障深度与工况组合形成的样本空间很大。最后，诊断模型一旦部署在边缘工业计算机上，还需满足实时性、通信稳定性和工况切换响应速度等工程约束，仅依靠离线数据集精度无法充分说明其工业适用性。因此，如何在真实样本不足条件下生成与实际分布一致的振动样本，并在闭环实时环境中验证诊断模型，是轴承智能诊断走向工业应用必须解决的问题。

### 1.2 现有技术的局限

传统数据增强方法通常基于加噪、平移、裁剪或频域扰动，能够扩充样本数量，但难以生成未观测工况下具有合理故障频率、冲击间隔和幅值分布的新样本。生成对抗网络（GAN）可通过对抗训练学习真实数据分布 [7]，但训练过程易出现模式崩溃，且一维振动信号在不同工况下具有复杂的非平稳调制特征，判别器难以稳定衡量物理一致性。变分自编码器（VAE）具有连续潜空间和稳定训练优点 [8]，但其重构目标常导致高频冲击成分被平滑，影响故障特征保真度。

扩散模型通过逐步加噪与逆向去噪学习复杂分布，在图像和时间序列生成中表现出较强的稳定性 [9]-[11]。然而，普通扩散模型若仅以数据驱动方式训练，仍可能忽略轴承动力学中由转速、故障部位和几何参数共同决定的故障特征频率。此外，现有诊断研究大多停留在离线准确率比较，缺少在硬件在环平台上对实时通信、滑窗推理、投票决策、端到端延迟和动态工况切换的联合验证。因此，仅证明生成样本“看起来相似”或在随机划分测试集上取得较高准确率，并不足以支撑工业极端工况下的可靠部署。

### 1.3 本文主要贡献

本文围绕“真实样本稀缺条件下的轴承故障生成增强与 HIL 工程验证”开展研究，主要贡献如下。

1. 提出一种物理引导的多源条件扩散生成网络 PhysiNet。该网络将转速、负载等离散工况信息编码为条件 token，并引入基于轴承动力学方程生成的连续机理信号，通过 Transformer 编码器实现异构条件的跨模态融合。与仅拼接条件向量的生成模型相比，该策略能够在扩散去噪过程中显式注入与故障频率和冲击模式相关的物理先验。

2. 构建基于可微分连续小波变换的物理驱动分解模块。该模块在 U-Net 高维特征空间中依据尺度能量占比动态保留主要故障分量，并将其与数据驱动残差特征重加权融合，使模型兼顾物理可解释性与非线性表达能力。

3. 引入流形对齐散度损失。该损失在批内样本空间构建真实信号的高斯联合概率分布和生成预测信号的 Student-t 联合概率分布，并以 KL 散度约束二者一致，从而减少生成信号在低维分布结构上的偏移。

4. 搭建完整的 HIL 实时诊断验证系统。系统基于 Speedgoat/Simulink 与工业计算机之间的 UDP 通信，每个报文包含 1 个真实标签 double 和 128 个振动采样 double，8 个报文组成 1024 点推理窗口。在线端实现有界队列、标签切换清空缓冲、滑窗推理、多数投票、端到端延迟统计和分类器头特征保存，可对多种诊断网络进行统一部署评估。

5. 设计面向极端工况的生成增强评估策略。本文采用曼哈顿距离梯度配比构建混合训练集，使中心工况保留较多真实样本，而边缘工况逐渐由生成样本补足，以模拟工业现场中极端工况真实样本不足的问题，并比较混合训练与纯真实训练在离线测试和 HIL 测试中的差异。

## 2 相关研究

### 2.1 现有生成网络

GAN 通过生成器与判别器之间的最小最大博弈学习目标分布，在故障样本扩增中得到广泛应用 [7]。其优势是采样速度快，能够生成较清晰的局部波形结构；不足是训练不稳定，且生成样本覆盖性依赖判别器的表达能力。对于轴承振动信号而言，不同转速和负载会引起故障频率、谐波能量、冲击间隔和噪声背景变化，GAN 容易在少样本工况下学习到有限模式。

VAE 通过编码器将输入映射到潜变量分布，再由解码器重构样本 [8]。其连续潜空间适合插值与条件控制，但均方重构项倾向于生成平滑波形，可能削弱冲击类故障的尖峰特征。近年来，时间序列扩散 Transformer 和流匹配模型逐渐成为生成建模研究热点 [11], [12]。扩散 Transformer 通过自注意力捕获长程依赖，适合复杂非平稳信号建模；流匹配模型则直接学习从噪声分布到数据分布的连续速度场，具有较快采样潜力。然而，这类模型若缺少机理约束，仍难以保证在未观测工况下的物理一致性。

### 2.2 扩散网络的工作原理

去噪扩散概率模型（DDPM）将数据生成过程分解为前向扩散和反向去噪两个马尔可夫链 [9], [10]。给定真实样本 \(x_0\)，前向过程在 \(T\) 个时间步内逐步加入高斯噪声：

$$
q(x_t|x_{t-1})=\mathcal{N}(x_t;\sqrt{1-\beta_t}x_{t-1},\beta_t I),
$$

其中 \(\beta_t\) 为噪声调度系数。利用 \(\alpha_t=1-\beta_t\) 和 \(\bar{\alpha}_t=\prod_{s=1}^{t}\alpha_s\)，可直接从 \(x_0\) 采样任意时间步的带噪样本：

$$
x_t=\sqrt{\bar{\alpha}_t}x_0+\sqrt{1-\bar{\alpha}_t}\epsilon,\quad \epsilon\sim\mathcal{N}(0,I).
$$

反向过程学习条件分布 \(p_\theta(x_{t-1}|x_t)\)，通过神经网络预测噪声、原始信号或速度参数。本文实现中采用 \(v\)-parameterization 目标：

$$
v_t=\sqrt{\bar{\alpha}_t}\epsilon-\sqrt{1-\bar{\alpha}_t}x_0,
$$

基础扩散损失为预测输出与目标 \(v_t\) 之间的均方误差，并按信噪比进行时间步加权。采样时从标准高斯噪声出发，逐步执行反向去噪获得目标工况下的振动信号。

### 2.3 轴承机理模型

本文使用八自由度轴承动力学模型生成物理先验信号。模型考虑转轴和轴承座在水平、垂直方向的位移与速度状态，接触力由 Hertz 接触刚度、滚动体位置、径向游隙和故障几何尺寸共同决定 [13], [14]。对于第 \(j\) 个滚动体，其角位置可写为

$$
\theta_j=\frac{2\pi}{N_b}(j-1)+\omega_c t,
$$

其中 \(N_b\) 为滚动体数量，\(\omega_c\) 为保持架角速度。接触变形由内外圈相对位移、游隙和局部故障深度共同决定：

$$
\gamma_j=(x_1-x_3)\cos\theta_j+(y_1-y_3)\sin\theta_j-\delta-H_j.
$$

当 \(\gamma_j>0\) 时发生接触，接触力为 \(k_b\gamma_j^{1.5}\)，否则接触力为零。根据故障类型，\(H_j\) 分别由内圈、外圈或滚动体故障角位置决定。模型进一步计算外圈故障频率 BPFO、内圈故障频率 BPFI 和滚动体故障频率 BPFB：

$$
BPFO=\frac{N_b N}{120}\left(1-\frac{D}{D_m}\right), \quad
BPFI=\frac{N_b N}{120}\left(1+\frac{D}{D_m}\right),
$$

$$
BPFB=\frac{D_m N}{60D}\left(1-\left(\frac{D}{D_m}\right)^2\right).
$$

在本文框架中，机理模型以故障部位、故障尺寸和转速为输入，求解常微分方程后截除初始瞬态，并取轴承座垂直方向加速度作为连续物理先验信号。该信号被统一重采样至固定长度并归一化到 \([-1,1]\)，用于后续条件嵌入。与仅利用标签或工况标量的条件生成方法相比，机理信号保留了故障冲击、特征频率及其谐波成分，为扩散模型提供了与轴承结构参数相关的物理约束。

## 3 多源条件扩散模型

### 3.1 总体架构：PhysiNet

所提 PhysiNet 是面向一维振动信号的物理引导扩散去噪网络。输入为带噪振动片段 \(x_t\in\mathbb{R}^{1\times L}\)、扩散时间步 \(t\)、工况条件向量 \(c\) 以及可选物理先验信号 \(s_p\)。网络首先通过一维卷积将 \(x_t\) 提升到高维特征 \(v_0(x_t)\)。随后，异构条件联合嵌入网络将转速、负载条件与物理先验序列融合为条件嵌入，并与时间嵌入相加。物理驱动分解模块从 \(v_0(x_t)\) 中提取故障相关分量 \(P_{i,t}(x)\)，U-Net 分支则对残差 \(v_0(x_t)-P_{i,t}(x)\) 建模。最终，网络通过重加权和投影层输出扩散预测量。

与普通 1D U-Net 扩散模型相比，PhysiNet 的关键差异在于三点：第一，条件信息不作为额外通道简单拼接，而是在时间嵌入空间中影响所有去噪层；第二，机理信号以连续序列形式进入 Transformer 编码器，保留物理时序结构；第三，U-Fourier/Wavelet 模块在高维特征空间显式分离物理故障分量，提高生成过程对故障冲击和频率结构的敏感性。

### 3.2 异构条件联合嵌入网络

本文将转速和负载归一化为二维条件向量：

$$
c=[(rpm-1000)/2000,\; load/60].
$$

条件向量经多层感知机映射为维度为 \(d\) 的 Condition Token：

$$
z_c=\text{MLP}(c).
$$

物理先验信号 \(s_p\) 通过一维卷积提升为物理序列特征 \(V_p\in\mathbb{R}^{L\times d}\)。随后，将 Condition Token 作为 class token 与物理序列拼接：

$$
Z=[z_c;V_p]+E_{pos},
$$

其中 \(E_{pos}\) 为可学习位置编码。拼接序列输入 Transformer 编码器：

$$
\tilde{Z}=\text{TransformerEncoder}(Z).
$$

编码后的 class token \(\tilde{z}_c\) 经线性层映射到时间嵌入维度，并与扩散时间嵌入相加：

$$
e_t^{'}=e_t+W_c\tilde{z}_c.
$$

该设计使离散工况条件能够主动从物理机理序列中聚合与当前工况相关的时频特征，而不是仅依赖标量条件调制。对于轴承振动信号而言，转速决定故障冲击的重复频率，负载影响接触刚度和冲击幅值，故障部位与损伤深度决定调制方式和能量分布。将这些信息在统一的 token 空间中融合，有助于模型在未观测工况下保持合理的时频结构。

### 3.3 物理驱动分解模块

PhysiNet 中的 U-Fourier/Wavelet Layer 使用 Morlet 小波的频域可微实现对高维特征进行连续小波变换 [15]。给定调制后的特征 \(X\)，其小波系数为

$$
W_X(a,\tau)=\mathcal{CWT}(X),
$$

其中 \(a\) 表示尺度，\(\tau\) 表示时间位置。对每个尺度计算能量：

$$
E_a=\sum_{\tau}W_X(a,\tau)^2.
$$

随后按能量降序排列并计算累积能量占比：

$$
\rho_k=\frac{\sum_{i=1}^{k}E_i}{\sum_i E_i}.
$$

本文保留累积能量达到阈值 \(\eta=0.9\) 前后的主要尺度分量，并通过简化逆小波重构获得物理驱动故障分量：

$$
P_{i,t}(x)=\mathcal{ICWT}(M\odot W_X).
$$

网络进一步对 \(P_{i,t}(x)\) 做一维卷积重加权，并与 U-Net 残差分支输出融合：

$$
C_{i,t}(x)=\sigma_2(D_{i,t}(x)+\sigma_1(WP_{i,t}(x))),
$$

最后由投影层得到去噪预测。该模块使模型在不同扩散时间步和工况条件下自适应选择能量显著的故障尺度，有利于保留冲击、调制和谐波结构。由于连续小波变换能够同时表征时间局部性与频率分布，物理驱动分解模块比单纯频域滤波更适合处理轴承振动中的非平稳冲击。尤其在低速或轻载条件下，故障冲击间隔变长、幅值减弱，小波尺度选择机制能够增强对弱周期分量的捕获能力。

### 3.4 流形对齐散度损失

扩散模型的基础训练目标为

$$
\mathcal{L}_{diff}=\mathbb{E}_{x_0,t,\epsilon}\left[\left\|f_\theta(x_t,t,c,s_p)-v_t\right\|_2^2\right].
$$

为进一步约束生成信号与真实信号在批内分布结构上的一致性，本文构建流形对齐散度。该设计借鉴了邻域保持和低维流形对齐思想 [16], [17]。对于一个批次中的真实样本 \(x_0^i\) 和网络还原得到的 \(\hat{x}_0^i\)，先将其展平到向量空间。真实样本之间的相似度由高斯核得到：

$$
r_{ij}=\frac{\exp(-\|x_i-x_j\|^2/2\sigma^2)}{\sum_{k\ne i}\exp(-\|x_i-x_k\|^2/2\sigma^2)}.
$$

对称化后得到真实联合概率：

$$
R_{ij}=\frac{r_{ij}+r_{ji}}{2B}.
$$

预测样本之间的低维邻域结构使用 Student-t 分布表示：

$$
T_{ij}=\frac{(1+\|\hat{x}_i-\hat{x}_j\|^2)^{-1}}
{\sum_{k\ne l}(1+\|\hat{x}_k-\hat{x}_l\|^2)^{-1}}.
$$

流形对齐损失定义为

$$
\mathcal{L}_{MA}=D_{KL}(R\|T)=\sum_{i\ne j}R_{ij}\log\frac{R_{ij}}{T_{ij}}.
$$

最终训练目标为

$$
\mathcal{L}=\mathcal{L}_{diff}+\lambda\mathcal{L}_{MA},
$$

其中当前实现中 \(\lambda=0.01\)。该约束使生成预测不仅在单个样本层面接近真实信号，也在批内邻域关系上与真实数据保持一致。

## 4 实验设计与硬件在环工程验证

### 4.1 实验配置与数据评价体系

本文使用 SDUST 轴承数据集作为真实数据来源。实验覆盖 10 类健康状态：正常状态、三种内圈故障深度、三种外圈故障深度和三种滚动体故障深度。每类数据按转速和负载工况组织，采集信号取单通道振动序列。生成模型和诊断模型统一采用 1024 点窗口；生成模型训练阶段使用重叠滑窗以增加可学习样本数，诊断模型训练阶段采用非重叠窗口以降低相邻样本相关性，并对每个窗口执行标准化处理。

生成模型对比建议包括 WGAN、CVAE、无物理先验扩散模型和本文 PhysiNet。生成质量从三个层面评价：第一，时域波形、频谱和包络谱对比，观察故障冲击和特征频率是否保真；第二，使用 FID 或基于诊断特征提取器的 Fréchet 距离衡量真实与生成分布差异；第三，采用“生成数据训练、真实数据测试”的分类器准确率评价生成样本对诊断边界的可用性。

诊断模型对比包括标准一维卷积网络、Patch-Transformer、时间卷积网络、轻量化深度可分离卷积网络、残差网络、ShuffleNet、Conformer、多尺度卷积网络、结构重参数化卷积网络和随机森林。所有深度模型均接收固定长度的一维振动窗口，并输出 10 类健康状态概率。为保证公平比较，不同模型采用相同训练/验证/测试划分，并依据模型容量设置合适的学习率、权重衰减、学习率预热、梯度裁剪、标签平滑和早停策略。

### 4.2 离线模型训练与泛化能力分析

为避免同一原始记录内部相邻窗口同时出现在训练集和测试集中造成时序泄露，本文优先采用文件级划分。当每个工况文件数不足以严格文件级划分时，采用时序分段回退策略，即训练集取前 70% 窗口、验证集取中间 10% 窗口、测试集取后 20% 窗口，保证每个窗口只属于一个集合。验证集和测试集均保持为纯真实数据，用于客观评估模型泛化能力。

混合训练集采用曼哈顿距离梯度配比策略。以 \(1800\) rpm 和 \(40\) load 为中心工况，将转速距离和负载距离相加得到工况距离 \(d\)。真实样本比例定义为

$$
f_{real}=\max(0,0.5-0.125d).
$$

因此，中心工况 \(d=0\) 时真实样本占比为 50%，最边缘工况 \(d=4\) 时真实样本占比为 0%，其余训练窗口由生成数据补足。每个工况保持相同的目标训练窗口数，以避免样本量差异造成类别或工况偏置。混合模型使用真实与生成样本共同训练，纯真实模型仅使用相同索引中可获得的真实样本训练，两者共享同一纯真实验证集和测试集。

离线泛化结果从整体测试准确率、核心/过渡/边缘工况分组准确率、混淆矩阵和不同架构推理成本等方面进行评价。核心工况用于检验模型在样本相对充分区域的拟合能力，边缘工况则用于衡量生成数据对真实样本空缺的补偿效果。若混合模型在边缘工况中明显优于纯真实模型，同时在核心工况中未出现显著退化，则说明生成样本能够有效扩展决策边界，而不是引入破坏性噪声。

### 4.3 HIL 实验系统构建

本文构建 Speedgoat 实时仿真机与工业计算机组成的 HIL 诊断平台。实时仿真端预先加载按故障类别、负载和转速组织的振动样本矩阵。每个故障类别对应一个数据块，块内保存负载向量、转速向量以及每个工况下多条 1024 点信号。在线运行时，实时仿真端根据目标负载和目标转速寻找归一化欧氏距离最近的工况格点，并在并列最近样本中均匀随机选择一条用于发送。

每条 1024 点振动信号被拆分为 8 个 UDP 报文发送。每个报文由 129 个双精度浮点数组成，第 1 个数为真实标签，后 128 个数为振动采样，因此单包长度为 \(129\times8=1032\) 字节。工业计算机端采用双进程架构：接收进程负责收包、校验长度、解析标签和振动数据，并将数据放入有界队列；推理进程维护 1024 点缓冲，窗口满后执行模型推理。

为保证动态切换时诊断窗口不跨故障类别，系统在接收端和推理端均实现标签变化检测。一旦真实标签变化，接收端清空队列中未处理数据，推理端清空样本缓冲和投票队列。在线诊断输出包括单次预测、长度为 3 的多数投票最终诊断、端到端延迟、每类准确率和会话汇总统计。系统还可保存分类器最后一层 Linear 之前的特征向量，用于离线与在线特征的 t-SNE 对比。

### 4.4 极端工况实验结果

极端工况实验面向真实数据稀缺的低速低载和边界转速负载组合。实验流程为：首先在离线阶段分别训练纯真实模型和混合增强模型；随后在 HIL 平台中固定目标工况和故障类别，令 Speedgoat 按报文格式连续发送样本；PC 端记录每类 500 次推理的单次准确率、投票后最终准确率和平均端到端延迟。

建议论文中重点展示以下现象。第一，纯真实模型在核心工况上通常具有较高准确率，但在边缘工况上出现明显下降，说明真实样本覆盖不足导致模型决策边界外推能力有限。第二，混合增强模型在边缘工况中准确率更稳定，尤其对故障深度相近的内圈、外圈和滚动体类别具有更低混淆。第三，轻量化模型如 CNN、ShuffleNet 和 MobileNet 可获得较低推理延迟，而 Conformer、MSDCNN 或 RepVGG 在精度上更具优势，适合根据工业现场实时性约束进行选型。

在结果分析中，应重点比较低速低载等极端工况下纯真实训练模型与混合增强模型的差异。若混合增强模型在最终诊断准确率、类别召回率和工况分组准确率上均优于纯真实模型，同时平均端到端延迟仍满足实时诊断要求，则可以说明 PhysiNet 生成样本在补充边缘工况数据方面具有实际工程价值。对于结构重参数化模型，还应单独讨论其训练阶段多分支表达能力和部署阶段单分支推理效率之间的平衡。

### 4.5 变工况动态测试

变工况动态测试用于验证诊断系统在工况切换过程中的响应能力。实时仿真端通过改变目标负载、目标转速和故障类别控制发送样本。当负载、转速或故障类别变化时，发送端重新选择最近格点样本并重置包序，接收端和推理端在标签变化时同步清空缓冲，可避免由跨类别拼窗导致的误诊。

在该实验中，建议设置从核心工况到边缘工况、再返回核心工况的阶跃序列，比较纯真实模型与混合增强模型在切换后的恢复时间和稳定准确率。评价指标包括切换后前 \(K\) 个窗口内的瞬态误诊次数、达到稳定准确率所需窗口数、最终投票准确率和平均端到端延迟。若混合增强模型在切换后更快恢复并保持较高准确率，则说明生成数据不仅改善静态极端工况识别，也提升了动态变工况鲁棒性。

### 4.6 讨论

所提方法的核心价值在于将“可生成”与“可信诊断”联系起来。对于轴承振动信号，生成样本的时域相似性并不必然意味着诊断有效性。若生成模型仅复制局部波形纹理，而未能保持故障特征频率、包络谱峰值和工况相关幅值变化，则增强数据可能会误导分类器。本文通过物理先验信号、条件 Transformer 融合和流形对齐损失共同约束生成过程，使生成样本不仅在单点波形上接近真实样本，也在批内邻域结构上保持与真实数据一致。

从诊断模型角度看，不同网络对生成数据的利用能力存在差异。卷积网络具有较强局部模式识别能力和较低推理成本，适合实时边缘部署；Transformer 与 Conformer 能够捕获长程周期依赖，但对样本数量和正则化更敏感；多尺度卷积网络对冲击宽度和周期变化具有更强适应性；结构重参数化网络则兼顾训练阶段表达能力和部署阶段低延迟。因此，HIL 结果不应仅以离线准确率排序，还应结合端到端延迟、工况切换稳定性和边缘硬件资源进行综合评价。

本文仍存在若干可进一步改进之处。首先，当前物理机理模型主要描述典型滚动轴承故障动力学，对于安装松动、复合故障和传感器安装差异的覆盖仍有限。其次，扩散模型采样过程相对较慢，若需要在线生成或快速扩充大规模工况网格，可进一步研究 DDIM、蒸馏扩散或流匹配采样策略。最后，HIL 平台虽然比离线测试更接近工程部署，但仍需在更长周期真实工业运行数据上验证模型的跨设备、跨载荷谱和跨噪声环境泛化能力。

## 5 总结

本文针对工业轴承故障诊断中真实样本稀缺、极端工况难采集和离线验证不足的问题，提出了物理引导的多源条件扩散生成网络 PhysiNet，并构建了完整的硬件在环实时诊断验证平台。PhysiNet 通过异构条件联合嵌入网络融合转速、负载与轴承动力学机理信号，通过可微分小波分解模块提取物理故障分量，并利用 KL 散度形式的流形对齐损失约束生成信号与真实信号的分布结构一致性。实验设计进一步采用曼哈顿距离梯度配比构建混合训练集，模拟工业场景中核心工况样本较多而边缘工况样本缺失的现实情况。

从工程实现角度，本文 HIL 平台将 Speedgoat 实时仿真、UDP 报文通信、工业计算机在线推理、多数投票和端到端延迟统计集成到统一流程中，使生成数据增强方法能够在接近实际部署的环境中被验证。该框架不仅可用于比较不同生成模型的样本质量，也可用于评估多种诊断网络在实时边缘部署中的可靠性。未来工作可进一步从三个方面展开：第一，引入更多真实工业现场数据，验证模型跨设备和跨传感器泛化能力；第二，将流匹配模型与物理条件编码结合，降低扩散采样开销；第三，将 HIL 平台扩展到多传感器融合和闭环控制场景，实现故障诊断与运维决策的一体化验证。

## 参考文献

[1] R. B. Randall and J. Antoni, “Rolling element bearing diagnostics: A tutorial,” Mechanical Systems and Signal Processing, vol. 25, no. 2, pp. 485-520, 2011.

[2] Y. Lei, J. Lin, Z. He, and M. J. Zuo, “A review on empirical mode decomposition in fault diagnosis of rotating machinery,” Mechanical Systems and Signal Processing, vol. 35, no. 1-2, pp. 108-126, 2013.

[3] W. Zhang, G. Peng, C. Li, Y. Chen, and Z. Zhang, “A new deep learning model for fault diagnosis with good anti-noise and domain adaptation ability on raw vibration signals,” Sensors, vol. 17, no. 2, 2017.

[4] F. Jia, Y. Lei, J. Lin, X. Zhou, and N. Lu, “Deep neural networks: A promising tool for fault characteristic mining and intelligent diagnosis of rotating machinery with massive data,” Mechanical Systems and Signal Processing, vol. 72-73, pp. 303-315, 2016.

[5] S. Bai, J. Z. Kolter, and V. Koltun, “An empirical evaluation of generic convolutional and recurrent networks for sequence modeling,” arXiv preprint arXiv:1803.01271, 2018.

[6] A. Vaswani et al., “Attention is all you need,” in Advances in Neural Information Processing Systems, pp. 5998-6008, 2017.

[7] I. Goodfellow et al., “Generative adversarial nets,” in Advances in Neural Information Processing Systems, pp. 2672-2680, 2014.

[8] D. P. Kingma and M. Welling, “Auto-encoding variational Bayes,” in International Conference on Learning Representations, 2014.

[9] J. Ho, A. Jain, and P. Abbeel, “Denoising diffusion probabilistic models,” in Advances in Neural Information Processing Systems, vol. 33, pp. 6840-6851, 2020.

[10] A. Q. Nichol and P. Dhariwal, “Improved denoising diffusion probabilistic models,” in International Conference on Machine Learning, pp. 8162-8171, 2021.

[11] Y. Song, J. Sohl-Dickstein, D. P. Kingma, A. Kumar, S. Ermon, and B. Poole, “Score-based generative modeling through stochastic differential equations,” in International Conference on Learning Representations, 2021.

[12] Y. Lipman, R. T. Q. Chen, H. Ben-Hamu, M. Nickel, and M. Le, “Flow matching for generative modeling,” in International Conference on Learning Representations, 2023.

[13] N. Tandon and A. Choudhury, “A review of vibration and acoustic measurement methods for the detection of defects in rolling element bearings,” Tribology International, vol. 32, no. 8, pp. 469-480, 1999.

[14] J. S. Rao, “Vibratory condition monitoring of machines,” CRC Press, 2000.

[15] S. Mallat, A Wavelet Tour of Signal Processing: The Sparse Way, 3rd ed. Academic Press, 2008.

[16] L. van der Maaten and G. Hinton, “Visualizing data using t-SNE,” Journal of Machine Learning Research, vol. 9, pp. 2579-2605, 2008.

[17] G. Hinton and S. Roweis, “Stochastic neighbor embedding,” in Advances in Neural Information Processing Systems, pp. 857-864, 2002.
