最重要的是先把条件注入加进去，加入平台里，可以可视化；暂时不管效果好坏！！！

1.12-1.18
1、截取数据，去除前5000个采样点                                                         (已找到修改位置)
2、denoising_diffusion_pytorch_1d    910
                         with torch.no_grad():
                            milestone = self.step // self.save_and_sample_every  #计算当前采样/保存的批次编号
                            batches = num_to_groups(self.num_samples, self.batch_size) #将总样本数 num_samples 分成多个批次
                            all_samples_list = list(map(lambda n: self.ema.ema_model.sample(batch_size=n), batches)) #生成样本

                        all_samples = torch.cat(all_samples_list, dim = 0)

                        torch.save(all_samples, str(self.results_folder / f'sample-{milestone}.png'))
                        self.save(milestone)
修改生成代码的图片格式，现在图片打不开                                                        (已完成)

1.19-1.26
1、读懂代码运行过程:复现该论文DiffPhysiNet之后的代码执行过程和网络框架
2、评价指标：到底生成了什么数据(1024个点)，VF到底是真实数据的那一段与生成数据比较  -- 解决方法：生成n个周期的数据，而后与真实数据任意n个周期的数据进行比较
每个样本生成了1024个点的振动数据，VF计算代码中，取min（真实数据长度，生成数据长度），即1024个点进行fft，并且考虑到了前9个生成样本。（可以进行更改以考虑更多样本，但考虑样本越多VF越大）
VF降低有可能是UCF筛选导致的，不能说明傅里叶算子有效。（实验结果：过滤后的VF和过滤前的VF相差不大）
t-sne降维图、判别器、下游故障诊断任务、VF
3、（已完成）数据归一化到【0，1】会引入0.5的直流分量，尝试归一化到【-1，1】。是否额外加入去直流问题问gpt。
需要归一化到-1，1；尝试后效果不错。
4、每一个保存的模型都进行一次VF计算，并显示其VF的值，推理程序中用VF最小的程序。