Unet为去噪模型，GaussianDiffusion1D为加噪模型
请你以资深深度学习与信号处理工程师的角色，使用 PyTorch、matplotlib、scikit-learn 和 scipy 等库，为我编写一个包含以下三个核心评估模块的完整代码库：




### 模块一：时频域物理特征对比分析 (Time & Frequency Domain Analysis)
请编写一个对比分析脚本，用于直观量化生成信号是否保留了真实的物理驱动特征。
1. **时域分析**：编写函数，在同一张图中绘制“真实信号”与“生成信号”的时域波形图，要求时间轴（x轴）映射为真实时间（采样率 fs=25600Hz）。
2. **频域分析（FFT）**：编写函数，对真实信号和生成信号进行快速傅里叶变换（FFT），计算其频谱幅值。
3. **可视化重点**：绘制频谱对比图时，请特别关注并放大**低频频段**（因为大部分轴承损伤特征频率集中在低频），并将真实信号与生成信号的频谱图进行重叠对比（设置不同的颜色和透明度 alpha），添加图例以证明两者在低频带的高度重合性。

### 模块二：下游故障诊断分类任务 (Downstream Fault Diagnosis Application)
这是验证生成数据实用性和质量的核心任务。请编写一个用于故障诊断的 1D CNN 分类器及完整的训练/测试验证流程。

1. **模型搭建**：
   构建一个一维卷积神经网络（1D CNN）。网络结构请参考常规的振动信号诊断模型：
   - 包含多层 `Conv1d`（例如通道数逐层递增，如 16 -> 32 -> 64 -> 128）。
   - 每层卷积后配合 `BatchNorm1d` 和 `ReLU` 激活函数。
   - 使用 `MaxPooling1d` 进行特征降维。
   - 最后使用 `Flatten` 展平，并连接全连接层（`Linear`）输出不同故障类别的预测概率。

2. **数据预处理与加载**：
   编写灵活的 `Dataset` 和 `DataLoader`，支持以 `.npy` 或其他常见格式读取一维序列数据。需要实现加载以下三个独立的数据集：
   - **真实数据（训练集）**：包含各类故障的真实采样数据。
   - **生成数据（训练集）**：利用扩散模型生成的对应故障类型的合成数据。
   - **真实数据（测试集）**：完全未参与过训练的真实数据，用于基准测试。

3. **训练与测试对比流程**：
   - **优化与损失**：编写标准的训练循环，必须使用 `CrossEntropyLoss` 作为损失函数，以及 `Adam` 优化器。
   - **评估循环**：编写评估函数，专门在**真实数据（测试集）**上计算分类准确率（Accuracy）。
   - **核心对比实验（TRTR vs TSTR）**：请在主函数或执行流程中，通过代码实例化并训练两个相同的 1D CNN 模型，要求最终输出这两者的性能对比：
     - **实验 A (TRTR)**：使用【真实数据（训练集）】训练模型，并在【真实数据（测试集）】上进行验证，记录最终的基准准确率。
     - **实验 B (TSTR)**：使用【生成数据（训练集）】训练模型，并在相同的【真实数据（测试集）】上进行验证，记录生成数据支持的准确率。
     - **结果量化**：在终端打印出这两者的 Accuracy 差距。如果生成数据训练的模型在测试集上的表现能接近真实数据训练的模型，即可量化证明生成数据质量极高。

### 模块三：T-SNE 特征降维与可视化 (T-SNE Feature Visualization)
请编写一个特征可视化脚本，用于直观地证明扩散模型生成的合成数据在特征空间中与真实数据的分布高度对齐，且生成数据没有偏离真实的物理特征规律。

1. **特征提取器构建**：
   - 请利用模块二中训练好的 1D CNN 模型（建议选择在**真实训练集**上训练好的 TRTR 模型，或者在**合成训练集**上训练的 TSTR 模型均可）作为特征提取器。
   - 编写代码或使用 PyTorch 的 `forward_hook`，截取该模型在最后一层全连接层（分类输出层）之前的**高维特征向量**（如经过 Flatten 层或倒数第二层全连接层后的输出）。

2. **多源特征提取与 T-SNE 降维**：
   - 将模块二中涉及的三个数据集：“真实数据（训练集）”、“生成数据（训练集）”以及“真实数据（测试集）”分别输入到特征提取器中，得到它们的高维特征。
   - 利用 `sklearn.manifold.TSNE` 将提取到的所有高维特征统一进行降维，映射到二维（2D）空间。请合理设置 T-SNE 的 `perplexity`（如 30-50）和 `n_iter` 以保证收敛。

3. **高质量散点图绘制**：
   - 使用 `matplotlib` 或 `seaborn` 绘制高质量的特征分布散点图。
   - **颜色映射（数据来源）**：使用两种对比鲜明的颜色来区分数据的来源，例如：
     * 颜色 A 代表 **真实数据（训练集）**
     * 颜色 B 代表 **生成数据（训练集）**
   - **图例与美化**：
     * 请添加清晰且分离的图例（最好分为“数据来源”和“故障类别”两组图例）。
     * 设置适当的散点透明度（alpha=0.6~0.8）以避免重叠遮挡。
   - **预期可视化效果**：如果生成质量高，图中应该能清晰地看出各个故障类别形成了各自独立的聚类簇，并且同一类别下，**生成数据（训练集）** 的散点应该与 **真实数据（训练集/测试集）** 的散点高度重合、完美对齐。

### 代码规范要求：
1. 代码结构要模块化，尽量分成 `dataset.py`、`models.py`、`evaluate_time_freq.py`、`train_diagnosis.py` 和 `visualize_tsne.py`，或给出一个结构清晰的 Jupyter Notebook 级大文件。
2. 为核心函数添加中文注释和 Type Hints（类型提示）。
3. 请考虑到 GPU (CUDA) 的加速计算。
4. 提供一个主函数或使用示例，演示如何传入 dummy 数据（如 `torch.randn` 生成的数据）跑通这三个流程。



你是一个资深的 PyTorch 深度学习专家。请帮我重构 `denoising_diffusion_pytorch_1d.txt` 文件中的 `PhysiNet` 类的条件注入网络。
目前在 `PhysiNet` 中，条件向量仅仅通过 `self.cond_mlp` 映射后与时间嵌入相加 (`t = t + cond_emb`)。我现在需要实现一个“基于 Class Token 的异构条件联合嵌入网络”。

具体重构要求如下：

1. 修改 `PhysiNet.__init__` 方法：
   - 移除原来的 `self.cond_mlp`。
   - 新增 `self.cond_fusion_mlp`：一个多层感知机（MLP），包含 `nn.Linear(cond_dim, init_dim)`、`nn.GELU()` 和 `nn.Linear(init_dim, init_dim)`。它的作用是将离散的工况特征（如 RPM、Load）融合为维度为 `init_dim` 的单一 Condition Token（即 Class Token）。
   - 新增位置编码模块 `self.cond_pos_emb`：由于接下来要对序列进行自注意力运算，需要为其添加位置编码。你可以实现一个简单的 1D 正弦位置编码（Sinusoidal Positional Encoding），支持的最大长度设为至少 2048（因为输入序列可能达到 1024，加上 1 个 Class Token）。
   - 新增 Transformer 编码器 `self.cond_transformer`：使用 `nn.TransformerEncoder` 和 `nn.TransformerEncoderLayer`。设置 `d_model=init_dim`, `nhead=4`, `dim_feedforward=init_dim * 4`, `batch_first=True`，层数设置为 1 或 2 层即可。
   - 新增特征对齐层 `self.cls_to_time`：使用 `nn.Linear(init_dim, time_dim)`，用于将 Transformer 输出的最终 Class Token 映射回 `time_dim`。

2. 修改 `PhysiNet.forward(self, x, time, cond=None, x_self_cond=None)` 方法的执行逻辑：
   - 第一步，先执行输入提升（Lifting）：将 `v0 = self.init_conv(x)` 提前到计算条件嵌入之前，得到形状为 `(B, init_dim, L)` 的物理机理特征序列。
   - 第二步，生成 Class Token：将 `cond` 经过 `self.cond_fusion_mlp` 处理，得到形状为 `(B, 1, init_dim)` 的初始 `Z_cond` Token。
   - 第三步，维度转换与级联拼接：将 `v0` 通过 `rearrange` 或 `.transpose(1, 2)` 转换为形状 `(B, L, init_dim)`。然后在序列维度（dim=1）上将 `Z_cond` 前置拼接到 `v0` 序列前方，构建出联合张量 `X_joint`，形状变为 `(B, L+1, init_dim)`。
   - 第四步，自注意力运算（深层融合）：为 `X_joint` 加上位置编码 `self.cond_pos_emb` 后，送入 `self.cond_transformer` 进行自注意力运算。此时，工况 Token 会自动吸附序列中关键的时频物理特征。
   - 第五步，全局 Context 提取与注入：切片提取第 0 个位置的输出 Token（即更新后的 Class Token），维度为 `(B, init_dim)`。将其通过 `self.cls_to_time` 映射为 `cond_emb`，维度变为 `(B, time_dim)`。
   - 第六步，进行条件注入：执行 `t = t + cond_emb`。
   - 后续代码（如 `p_it = self.u_fourier(v0, t)` 及其之后的 U-Net 处理）保持不变，依然使用原有的 `v0` 和 更新后的 `t` 进行去噪。

注意：
- 确保引入了必要的包（如 `import torch.nn as nn`）。
- 保持 `cond_dim == 0` 或 `cond is None` 时的无条件兼容性分支不崩溃。
- `train_1d_vibration.txt` 中的训练逻辑和 Dataset 返回格式保持不变，仅修改 `denoising_diffusion_pytorch_1d.txt`。
- 请给出完整的 `PhysiNet` 类的修改后代码。