Unet为去噪模型，GaussianDiffusion1D为加噪模型
请你以资深深度学习与信号处理工程师的角色，使用 PyTorch、matplotlib、scikit-learn 和 scipy 等库，为我编写一个包含以下三个核心评估模块的完整代码库：

### 模块一：时频域物理特征对比分析 (Time & Frequency Domain Analysis)
请编写一个对比分析脚本，用于直观量化生成信号是否保留了真实的物理驱动特征。
1. **时域分析**：编写函数，在同一张图或上下子图中绘制“真实信号”与“生成信号”的时域波形图，要求时间轴（x轴）映射为真实时间（例如采样率 fs=25600Hz）。
2. **频域分析（FFT）**：编写函数，对真实信号和生成信号进行快速傅里叶变换（FFT），计算其频谱幅值。
3. **可视化重点**：绘制频谱对比图时，请特别关注并放大**低频频段**（因为大部分轴承损伤特征频率集中在低频），并将真实信号与生成信号的频谱图进行重叠对比（设置不同的颜色和透明度 alpha），添加图例以证明两者在低频带的高度重合性。

### 模块二：下游故障诊断分类任务 (Downstream Fault Diagnosis Application)
这是验证生成数据实用性的核心任务。请编写一个用于故障诊断的 1D CNN 分类器及完整的训练/测试验证流程。
1. **模型搭建**：构建一个一维卷积神经网络（1D CNN）。网络结构请参考常规诊断模型：包含多层 `Conv1d`（例如逐层通道数递增、配合 BatchNorm1d 和 ReLU）、MaxPooling 降维，最后连接全连接层（`Linear`）输出故障类别概率。
2. **数据预处理与加载**：编写 `Dataset` 和 `DataLoader`，支持加载真实域数据、生成的未知工况数据作为训练集，并加载真实未知工况数据作为测试集。
3. **训练与测试流程**：
   - 编写训练循环（使用 CrossEntropyLoss 和 Adam 优化器）。
   - 编写评估循环，在**完全未见过的真实目标域（Target Domain）数据**上测试模型的分类准确率（Accuracy）。
   - 如果使用“真实+生成”数据训练的模型在测试集上表现优于仅使用“真实”数据训练的模型，则证明生成数据质量高。请在代码中保留这两种训练模式的对比接口。

### 模块三：T-SNE 特征降维与可视化 (T-SNE Feature Visualization)
请编写一个特征可视化脚本，用于证明生成的数据特征分布与真实数据对齐，且能捕捉到域不变特征（Domain-invariant features）。
1. **特征提取**：利用模块二中训练好的 1D CNN 模型作为特征提取器。请编写代码截取该模型分类器（如全连接层的前一层或第二层）输出的高维特征向量。
2. **T-SNE 降维**：将“真实源域数据”、“真实目标域数据”以及“生成的目标域数据”同时输入该特征提取器，得到高维特征后，使用 `sklearn.manifold.TSNE` 将其降维至二维（2D）。
3. **散点图绘制**：使用 `matplotlib` 或 `seaborn` 绘制高质量散点图。
   - 要求：使用不同的**颜色**代表不同的域（如 源域、真实目标域、生成域）。
   - 要求：使用不同的**标记形状（marker）**代表不同的故障类别（如 正常、内圈故障、外圈故障等）。
   - 添加清晰的图例，以展示生成的样本是否与真实目标域样本在同一类别簇中完美对齐。

### 代码规范要求：
1. 代码结构要模块化，尽量分成 `dataset.py`、`models.py`、`evaluate_time_freq.py`、`train_diagnosis.py` 和 `visualize_tsne.py`，或给出一个结构清晰的 Jupyter Notebook 级大文件。
2. 为核心函数添加中文注释和 Type Hints（类型提示）。
3. 请考虑到 GPU (CUDA) 的加速计算。
4. 提供一个主函数或使用示例，演示如何传入 dummy 数据（如 `torch.randn` 生成的数据）跑通这三个流程。